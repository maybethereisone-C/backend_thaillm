# --- Required ---
LLM_BACKEND=openai_compatible
LLM_UPSTREAM_BASE_URL=https://upstream.example.com/v1
LLM_UPSTREAM_API_KEY=your-upstream-api-key-here

# --- Model ---
LLM_MODEL_ID=your-model-id

# --- Gateway Auth (set before exposing to the internet) ---
LLM_API_KEYS=change-me-key-1,change-me-key-2

# --- Security ---
LLM_TRUSTED_HOSTS=your-domain.com
LLM_ALLOWED_ORIGINS=https://your-frontend.com

# --- Inference ---
LLM_MAX_TOKENS_DEFAULT=512
LLM_PROMPT_GUARD_ENABLED=true

# --- Server ---
# Container listen port. The deploy platform maps an external host:YOUR_PORT to this.
PORT=8000

# --- Competition (agent back-test) ---
# Directory containing the agent modules (agent.py, tools.py, ...); bundled at /agent/src in the image.
LLM_AGENT_SRC_PATH=/agent/src
# Path to the DuckDB database file the agent queries (mount it read-only at this path).
LLM_AGENT_DB_PATH=/data/agent.duckdb
# Per-request audit trail directory (must be writable by the app user).
LLM_AUDIT_TRAIL_DIR=/data/audit_trails
LLM_THAILLM_BASE_URL=https://thaillm-upstream.example.com/v1
LLM_THAILLM_MODEL_ID=your-thaillm-model-id
LLM_THAILLM_API_KEY=your-thaillm-api-key
