# --- Model server (local, OpenAI-compatible; no API key) ---
# Point this at the local llama-server serving the gguf. From inside Docker use
# the host/sidecar address (e.g. http://host.docker.internal:8080/v1).
LLM_THAILLM_BASE_URL=http://127.0.0.1:8080/v1
LLM_THAILLM_MODEL_ID=typhoon-s-thaillm-8b-instruct

# --- Security (set before exposing to the internet) ---
LLM_TRUSTED_HOSTS=your-domain.com
LLM_ALLOWED_ORIGINS=https://your-frontend.com

# --- Server ---
# Container listen port.
PORT=8000

# --- Agent paths (bundled defaults shown; override only if not using Docker) ---
LLM_AGENT_SRC_PATH=/agent/src
LLM_AGENT_DB_PATH=/data/agent.duckdb
LLM_AUDIT_TRAIL_DIR=/data/audit_trails
